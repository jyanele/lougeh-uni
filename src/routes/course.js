import express, { Router } from 'express'

const courseRoute = express.Router()

//TODO
courseRoute.get('/', )
courseRoute.get('/:id', )
courseRoute.post('/',)
courseRoute.patch('/:id')

export default courseRoute