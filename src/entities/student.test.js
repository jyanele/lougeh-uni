import { createStudent } from './'

describe('Student', () => {
    test.todo('must have id')
    test.todo('must have first name')
    test.todo('must have last name')
    test.todo('must have date of birth')
    test.todo('must have enrollment year')
})